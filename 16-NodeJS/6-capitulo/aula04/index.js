// setInerval -> irá rodar uma função N vezes depois de intervalos de X milissegundos

const timeOut = 1000
const cheking = () => console.log('checking!')

setInterval(cheking, timeOut)

//é preciso dar ctrl + c para parar a função no console
